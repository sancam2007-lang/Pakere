{
  "name": "pakere",
  "version": "0.1.0",
  "private": true,
  "description": "Pakere — a peer-to-peer private parking marketplace demo for Berkeley. Map-first driver booking, live sessions, and full host/driver dashboards.",
  "keywords": [
    "parking",
    "marketplace",
    "leaflet",
    "demo",
    "berkeley"
  ],
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "http-server src -p 8080 -c-1 -o",
    "dev": "http-server src -p 8080 -c-1 -o",
    "test": "node scripts/smoke-test.mjs",
    "lint": "node --check src/app.js"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "jsdom": "^24.0.0"
  }
}
